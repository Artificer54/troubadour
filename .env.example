# Copy this file to .env and adjust values as needed.
# .env is gitignored — never commit secrets.

# Port the Express server listens on (default: 3001)
# SERVER_PORT=3001

# Where to store the SQLite database and uploaded files.
# Defaults to the project root. Uncomment to move data out of the repo directory.
# DATA_DIR=./data
