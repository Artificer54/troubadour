node_modules
dist
.env
.env.local
.env.production
.env.*.local

# SQLite database files (project-root default location)
troubador.db
troubador.db-shm
troubador.db-wal

# Uploaded files (project-root default location)
images/
tracks/

# Optional data/ directory (if DATA_DIR=./data is set)
data/

# Build artifacts
releases/
server-bundle/

# Certificates
*.key
*.pem
*.p12
*.pfx
